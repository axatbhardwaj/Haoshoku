version: '3.8'
services:
  dashy:
    image: lissy93/dashy:latest
    container_name: dashy
    volumes:
      - ./conf.yml:/app/public/conf.yml
    ports:
      - 8080:80
    restart: unless-stopped
    environment:
      - NODE_ENV=production
