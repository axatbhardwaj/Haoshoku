# Commit Style

Semantic prefix: `feat`, `fix`, `refactor`, `docs`, `test`, `chore`. Subject ≤ 50 chars.

- One logical change per commit.
- Keep a feature with its test; split a feature from a drive-by refactor.
