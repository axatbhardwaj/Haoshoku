name = "Agents"
title = "agents"
color = "green"

# Single Claude pane. This was a claude | codex vertical split; Super+A now opens
# Claude alone and Codex is started by hand when it is actually wanted. With one
# pane there is no root split to declare — the pane is the whole tab.
[[panes]]
id = "claude"
type = "terminal"
directory = "/home/xzat"
commands = ["claude -r io"]
is_focused = true
