name = "Agents"
title = "agents"
color = "green"

# Dormant legacy single-Claude pane. Omarchy no longer binds Super+A to Haki or
# this agents layout. Codex is started by hand when it is actually wanted. With one
# pane there is no root split to declare — the pane is the whole tab.
[[panes]]
id = "claude"
type = "terminal"
directory = "/home/xzat"
commands = ["claude -r io"]
is_focused = true
