# Font (matches former ghostty config)
font_family      0xProto Nerd Font Mono
font_size        11

# Background / opacity / blur
# background_blur is honored by KWin via _KDE_NET_WM_BLUR_BEHIND_REGION.
# Set to 0 to disable; values 1-64 set the blur radius.
background_opacity   0.85
background_blur      1

# Colors: intentionally left blank — Caelestia injects the live scheme via
# OSC escape sequences at startup (see /etc/xdg/quickshell/caelestia/assets/wrap_term_launch.sh).
# Setting palette here would conflict with that runtime theming.

# Claude Code: shift+enter inserts a newline without submitting
map shift+enter send_text all \x1b\r
