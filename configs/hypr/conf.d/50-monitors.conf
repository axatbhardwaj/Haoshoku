# Monitor fallback. `hyprctl monitors -j` was unavailable when this release was
# cut, so v1 uses Hyprland's documented safe auto layout until a live topology
# can be captured from the real Hyprland session.
monitor = , preferred, auto, 1
